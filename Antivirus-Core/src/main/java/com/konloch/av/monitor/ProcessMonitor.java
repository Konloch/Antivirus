package com.konloch.av.monitor;

/**
 * @author Konloch
 * @since 6/25/2024
 */
public class ProcessMonitor
{
	//TODO watch processes for non-approved processes, lock them and wait until scan verified it's a non threat
	// alternatively we can enforce a strict whitelist only system where you need to manually approve the process in the GUI
}
